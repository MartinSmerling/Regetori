**README – Summary**

This project implements a Python-based robotic process automation (RPA) solution that automatically generates reports from Excel data. 
The program reads data from an Excel file, inserts it into a predefined Word template, converts the document into a PDF, 
and sends it via email to specified recipients. The goal is to eliminate repetitive manual work, reduce errors, and significantly 
speed up the report creation process.
